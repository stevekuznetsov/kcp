
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: workspacewritelocks.tenancy.kcp.dev
spec:
  group: tenancy.kcp.dev
  names:
    categories:
    - kcp
    kind: WorkspaceWriteLock
    listKind: WorkspaceWriteLockList
    plural: workspacewritelocks
    singular: workspacewritelock
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkspaceWriteLock is a checkpointing object to implement write
          locks on Workspaces.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceWriteLockSpec holds the intended state for the lock.
            properties:
              locked:
                description: Locked determines the intended state of this lock.
                type: boolean
              quorum:
                description: Quorum is the count of acknowledgements necessary for
                  a state change.
                type: integer
            required:
            - locked
            - quorum
            type: object
          status:
            description: WorkspaceWriteLockStatus holds the current observed state
              for the lock.
            properties:
              acknowledgements:
                description: Acknowledgements expose API server replicas acknowledging
                  the intended state.
                items:
                  description: WorkspaceWriteLockAcknowledgement records that an API
                    server replica is rejecting writes after some resource version.
                  properties:
                    identifier:
                      type: string
                    resourceVersion:
                      type: string
                  required:
                  - identifier
                  - resourceVersion
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - identifier
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
